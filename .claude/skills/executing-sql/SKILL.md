---
name: executing-sql
description: Executing SQL statement on the remote server running PostgreSQL.
allowed-tools: ["Bash(psql *)"]
---

To execute an SQL statement, run the following command:

```bash
psql -c '<SQL statement>'
```
